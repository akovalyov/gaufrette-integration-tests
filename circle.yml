machine:
  services:
    - docker
  php:
    version: 5.5.11

dependencies:
  override:
    - pecl install mongo
    - composer install --prefer-source --ignore-platform-reqs
    - docker-compose up -d

test:
  override:
    - php vendor/bin/behat -s default --tags="~@real"
