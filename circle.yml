machine:
  services:
    - docker
  php:
    version: 5.5.11

dependencies:
  override:
    - printf "\n" | pecl install mongo
    - printf "\n" | pecl install runkit
    - composer install --prefer-source --ignore-platform-reqs
    - docker-compose up -d

test:
  override:
    - php vendor/bin/behat -s default --tags="~@real"
